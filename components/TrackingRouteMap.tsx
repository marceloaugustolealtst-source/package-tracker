'use client';
import { useEffect, useMemo, useState } from 'react';
type Event={id:string;location:string|null;status:string;created_at?:string;description?:string;event_time?:string};
type Point={name:string;x:number;y:number};
const cities:Point[]=[{name:'Toronto',x:12,y:18},{name:'New York',x:18,y:27},{name:'London',x:48,y:20},{name:'Paris',x:51,y:27},{name:'Warsaw',x:55,y:22},{name:'Cairo',x:59,y:39},{name:'Dubai',x:65,y:43},{name:'Mumbai',x:72,y:56},{name:'Lagos',x:39,y:58},{name:'Nairobi',x:55,y:67},{name:'Johannesburg',x:45,y:78},{name:'São Paulo',x:25,y:78},{name:'Singapore',x:82,y:72},{name:'Tokyo',x:91,y:34},{name:'Sydney',x:88,y:88}];
const city=(v:string)=>cities.find(c=>(v||'').toLowerCase().includes(c.name.toLowerCase()))||null;
const distance=(a:Point,b:Point)=>Math.hypot(a.x-b.x,a.y-b.y);
const label=(s:string)=>s.replaceAll('_',' ');
export default function TrackingRouteMap({origin,destination,status,events}:{origin:string;destination:string;status:string;events:Event[]}){
 const start=useMemo(()=>city(origin)||cities.find(c=>c.name==='Lagos')!,[origin]);const end=useMemo(()=>city(destination)||cities.find(c=>c.name==='Warsaw')!,[destination]);
 const route=useMemo(()=>{const cs=cities.filter(c=>c.name!==start.name&&c.name!==end.name).filter(c=>distance(start,c)+distance(c,end)<=distance(start,end)*1.5).sort((a,b)=>distance(start,a)-distance(start,b));const pts=[start];const n=Math.min(3,cs.length);for(let i=0;i<n;i++){const p=cs[Math.floor((i+1)*cs.length/(n+1))];if(p)pts.push(p)}pts.push(end);return pts},[start,end]);
 const stopped=['processing','at_hub','exception','delivered'].includes(status);const [progress,setProgress]=useState(.12);
 useEffect(()=>{if(stopped)return;const t=setInterval(()=>setProgress(v=>v>=route.length-1?.05:v+.0025),70);return()=>clearInterval(t)},[stopped,route.length]);
 const segment=Math.min(Math.floor(progress),route.length-2),local=progress-segment,from=route[segment]||route[0],to=route[segment+1]||route[0];const x=from.x+(to.x-from.x)*local,y=from.y+(to.y-from.y)*local,angle=Math.atan2(to.y-from.y,to.x-from.x)*180/Math.PI;
 const current=stopped?(status==='delivered'?end:route[Math.min(segment,route.length-1)]):{name:from.name,x,y};const latest=events[events.length-1];const title=stopped?(status==='delivered'?'Delivered':status==='exception'?'Shipment exception':'At facility'):'In transit';
 return <section className="tracker-card"><div className="summary"><div><span className="eyebrow">SHIPMENT STATUS</span><h2>{title}</h2><p>{stopped?`Package is currently at ${current.name}`:`Your package is moving toward ${to.name}`}</p></div><span className={`pill ${stopped?'hold':'moving'}`}>{stopped?'AT CHECKPOINT':'● LIVE TRANSIT'}</span></div>
 <div className="route-bar"><div><small>FROM</small><b>{origin}</b></div><div className="arrow">→</div><div><small>TO</small><b>{destination}</b></div></div>
 <div className="map"><div className="map-title"><b>Shipment journey</b><span>{stopped?`At ${current.name}`:`In transit to ${to.name}`}</span></div><div className="map-grid"/><svg viewBox="0 0 100 100" preserveAspectRatio="none"><path className="route-bg" d={`M ${route.map((p,i)=>`${i?'L':'M'} ${p.x} ${p.y}`).join(' ')}`}/><path className="route-line" d={`M ${route.map((p,i)=>`${i?'L':'M'} ${p.x} ${p.y}`).join(' ')}`}/></svg>
 {route.map((c,i)=><div key={c.name} className={`city ${i===0?'origin':i===route.length-1?'destination':'checkpoint'}`} style={{left:`${c.x}%`,top:`${c.y}%`}}><i/><span>{c.name}</span></div>)}
 {!stopped&&<div className="moving-unit" style={{left:`${x}%`,top:`${y}%`,transform:`translate(-50%,-50%) rotate(${angle}deg)`}}><span className="direction-arrow">➤</span><span className="aircraft">✈</span></div>}{stopped&&<div className="hold-marker" style={{left:`${current.x}%`,top:`${current.y}%`}}>✓</div>}</div>
 {!stopped&&<div className="live-strip"><span className="pulse"/><b>IN TRANSIT</b><span>Package moving toward <strong>{to.name}</strong></span><span className="direction">➜</span></div>}
 <div className="timeline"><h3>Tracking history</h3>{events.length?events.slice().reverse().map((e,i)=><div className="event" key={e.id||i}><div className="event-dot"/><div><b>{label(e.status)}</b><span>{e.location||'Shipment facility'}</span>{e.event_time&&<small>{new Date(e.event_time).toLocaleString()}</small>}{e.description&&<p>{e.description}</p>}</div></div>):<div className="event"><div className="event-dot"/><div><b>{title}</b><span>{current.name}</span></div></div>}<div className="event"><div className="event-dot future"/><div><b>Destination</b><span>{destination}</span></div></div></div>
 <style jsx>{`.tracker-card{margin-top:18px;border:1px solid #e5e7eb;border-radius:14px;background:#fff;overflow:hidden;box-shadow:0 2px 10px rgba(16,24,40,.06)}.summary{padding:18px;display:flex;justify-content:space-between;gap:15px}.eyebrow{font-size:9px;font-weight:800;letter-spacing:.12em;color:#667085}.summary h2{margin:5px 0 3px;font-size:20px;text-transform:capitalize}.summary p{margin:0;color:#667085;font-size:12px}.pill{height:max-content;padding:7px 9px;border-radius:20px;font-size:9px;font-weight:900;white-space:nowrap}.moving{color:#087443;background:#e9f8ef}.hold{color:#a15c00;background:#fff3d6}.route-bar{margin:0 18px 14px;padding:12px;border:1px solid #eaecf0;border-radius:9px;display:flex;align-items:center;justify-content:space-between}.route-bar div:not(.arrow){display:flex;flex-direction:column;gap:3px}.route-bar small{font-size:8px;color:#667085;font-weight:800}.route-bar b{font-size:11px}.arrow{font-size:19px;color:#087443}.map{height:390px;position:relative;overflow:hidden;background:#f2f5f2;border-top:1px solid #eaecf0;border-bottom:1px solid #eaecf0}.map-title{position:absolute;z-index:8;left:12px;right:12px;top:12px;display:flex;justify-content:space-between;padding:9px 11px;background:rgba(255,255,255,.95);border-radius:8px;box-shadow:0 2px 8px rgba(16,24,40,.1)}.map-title b{font-size:11px}.map-title span{font-size:10px;color:#667085}.map-grid{position:absolute;inset:0;background-image:linear-gradient(rgba(60,80,60,.055) 1px,transparent 1px),linear-gradient(90deg,rgba(60,80,60,.055) 1px,transparent 1px);background-size:7% 10%}.map svg{position:absolute;inset:0;width:100%;height:100%;z-index:1}.route-bg{fill:none;stroke:white;stroke-width:3.2;stroke-linecap:round;stroke-linejoin:round}.route-line{fill:none;stroke:#087443;stroke-width:1.2;stroke-dasharray:2.5 2;stroke-linecap:round;stroke-linejoin:round}.city{position:absolute;z-index:4;transform:translate(-50%,-50%)}.city i{display:block;width:6px;height:6px;border-radius:50%;background:#667085;border:2px solid white;box-shadow:0 0 0 1px #667085}.city span{position:absolute;left:9px;top:-7px;white-space:nowrap;font-size:9px;font-weight:700;color:#344054;text-shadow:0 1px 2px #fff}.city.origin i,.city.destination i{width:9px;height:9px;background:#087443;box-shadow:0 0 0 2px #fff,0 0 0 3px #087443}.city.origin span,.city.destination span{font-weight:900}.moving-unit{position:absolute;z-index:9;transform-origin:center;transition:left .07s linear,top .07s linear}.aircraft{font-size:27px;color:#087443;text-shadow:0 1px 4px white}.direction-arrow{position:absolute;left:-27px;top:1px;font-size:18px;color:#087443;text-shadow:0 1px 3px white}.hold-marker{position:absolute;z-index:7;width:30px;height:30px;border-radius:50%;display:grid;place-items:center;background:#fff;color:#a15c00;border:3px solid #d99a22;transform:translate(-50%,-50%);box-shadow:0 0 0 6px rgba(217,154,34,.16)}.live-strip{min-height:30px;padding:7px 14px;background:#f6faf7;color:#087443;font-size:10px;display:flex;align-items:center;gap:7px}.pulse{width:7px;height:7px;border-radius:50%;background:#087443;box-shadow:0 0 0 4px rgba(8,116,67,.12);animation:pulse 1.2s infinite}@keyframes pulse{50%{opacity:.35;transform:scale(.75)}}.direction{margin-left:auto;font-size:17px}.timeline{padding:15px 18px 18px}.timeline h3{margin:0 0 13px;font-size:14px}.event{position:relative;display:flex;gap:11px;padding:0 0 15px 18px;border-left:1px solid #d0d5dd}.event:last-child{border-left-color:transparent}.event-dot{position:absolute;left:-5px;top:0;width:9px;height:9px;border-radius:50%;background:#087443;border:2px solid white;box-shadow:0 0 0 1px #087443}.event-dot.future{background:#fff;border-color:#98a2b3;box-shadow:0 0 0 1px #98a2b3}.event b{display:block;font-size:11px;text-transform:capitalize}.event span,.event small{display:block;margin-top:3px;color:#667085;font-size:10px}.event p{margin:4px 0 0;color:#667085;font-size:10px}@media(max-width:600px){.map{height:290px}.city span{font-size:7px}.city:not(.origin):not(.destination) span{display:none}.summary{padding:14px}.route-bar{margin:0 14px 12px}.timeline{padding:14px}.aircraft{font-size:23px}.direction-arrow{font-size:15px;left:-22px}}`}</style></section>;
}
